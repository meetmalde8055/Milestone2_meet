<%- include('../partials/header.ejs')%>

   <div class = "content">
    <div class="row">
      <div>Name:</div>
      <div class="col-9"><%= item.title%></div>
    </div>
    <div class="row">
      <div>Category:</div>
      <div class="col-9"><%= item.category%></div>
    </div>
    <div class="row">
      <div>Details:</div>
      <div class="col-9"><%= item.details%></div>
    </div>
    <div class="row">
      <div>Status:</div>
      <div class="col-9"><%= item.status%></div>
    </div>
      <div><img class="img-size" src="<%= item.image %>" alt="Logo"></div>
    </div>
    <form>
      <button type="submit" formmethod="GET" formaction="/trades/<%= item.id%>/edit">Edit</button>
      <button type="submit" formmethod="POST" formaction="/trades/<%= item.id%>?_method=DELETE">Delete</button>
    </form>
<%- include('../partials/footer.ejs')%>
